<ResourceDictionary x:Class="SolaceEditor.Dictionaries.ControlTemplates"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:SolaceEditor.Dictionaries">

    <Style TargetType="{x:Type TextBox}" x:Key="TextBoxStyle">
        <!-- TODO: design the visual look of the TextBox -->
    </Style>

    <Style TargetType="{x:Type TextBox}" x:Key="{x:Type TextBox}" BasedOn="{StaticResource TextBoxStyle}">
        <EventSetter Event="KeyDown" Handler="OnTextBox_KeyDown"/>
    </Style>
</ResourceDictionary>